<UserControl x:Class="GameDataViewer.App.Views.MonsterDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="260">

    <UserControl.Resources>
        <Style x:Key="PropLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Gray3}"/>
        </Style>
        <Style x:Key="PropValue" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- 빈 상태 -->
        <TextBlock x:Name="EmptyHint"
                   Text="몬스터를 선택하세요"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Opacity="0.4" FontSize="13" FontStyle="Italic"/>

        <!-- 상세 패널 -->
        <ScrollViewer x:Name="DetailPanel" Visibility="Collapsed"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="12,10">

                <!-- 등급 뱃지 + 이름 + BOSS 마커 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Border CornerRadius="3" Padding="6,3" Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            Background="{Binding Grade, Converter={StaticResource GradeToColor}}">
                        <TextBlock Text="{Binding Grade}" FontWeight="Bold" FontSize="11" Foreground="White"/>
                    </Border>
                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 지역 · 속성 부제목 + BOSS 뱃지 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="{Binding Location}" Opacity="0.6" Margin="0,0,6,0"/>
                    <TextBlock Text="·" Opacity="0.4" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding Element}" Opacity="0.6"/>
                    <!-- BOSS 뱃지 — IsBoss=True 일 때만 표시 -->
                    <Border CornerRadius="3" Padding="5,2" Margin="8,0,0,0"
                            Background="#CCE53935" VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBoss}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="BOSS" FontWeight="Bold" FontSize="10" Foreground="White"/>
                    </Border>
                </StackPanel>

                <Separator Margin="0,0,0,8"/>

                <!-- 스탯 Grid -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="72"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="26"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="레벨" Style="{StaticResource PropLabel}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Level}" Style="{StaticResource PropValue}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="HP" Style="{StaticResource PropLabel}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Hp, StringFormat={}{0:#,0}}" Style="{StaticResource PropValue}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="공격" Style="{StaticResource PropLabel}"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding AttackMin, StringFormat={}{0:#,0}}"/>
                        <TextBlock Text=" ~ " Opacity="0.45"/>
                        <TextBlock Text="{Binding AttackMax, StringFormat={}{0:#,0}}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="방어" Style="{StaticResource PropLabel}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Defense, StringFormat={}{0:#,0}}" Style="{StaticResource PropValue}"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="마법방어" Style="{StaticResource PropLabel}"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding MagicDefense, StringFormat={}{0:#,0}}" Style="{StaticResource PropValue}"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="경험치" Style="{StaticResource PropLabel}"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding ExpReward, StringFormat={}{0:#,0}}" Style="{StaticResource PropValue}"/>
                </Grid>

                <Separator Margin="0,8"/>
                <TextBlock Text="설명" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                           Opacity="0.75" LineHeight="18"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
